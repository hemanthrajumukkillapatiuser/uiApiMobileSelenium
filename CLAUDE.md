# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

**The full, tool-agnostic instructions live in [`AGENTS.md`](./AGENTS.md). Read that file and follow it as the source of truth** — it covers the operating mindset (act as a senior automation architect), build/test commands, the TestNG-only convention, and the framework architecture. Do not duplicate that content here; update `AGENTS.md` instead.
